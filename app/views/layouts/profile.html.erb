<!DOCTYPE html>
<html>
  <head>
    <title>AlphaState</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Bricolage+Grotesque:opsz,wght@12..96,200..800&family=Roboto+Serif:ital,opsz,wght@0,8..144,100..900;1,8..144,100..900&family=Roboto:ital,wght@0,100..900;1,100..900&display=swap" rel="stylesheet">
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-brand-100 font-sans">

    <% if login_or_signup(request.url)%>
      <%= render 'shared/header_devise'%>
    <% else %>
      <%= render 'shared/header'%>
    <% end %>
    <main>
      <div class="container mt-16">
        <div class="grid grid-cols-12 gap-4">
          <div class="col-span-12 md:col-span-8 p-4 border-r-2">
            <h2 class="font-sans font-bold text-4xl mb-10"><%= @user.username %></h2>
            <div class="flex items-center gap-8 *:text-brand-300 *:text-xl mb-8">
              <%= link_to 'Home', profile_path(@user.username), class: "relative focus:text-brand-400 hover:text-brand-400 transition-all duration-300 after:content-[''] after:absolute after:left-0 after:bottom-0 after:w-full after:h-[2px] after:bg-brand-400 after:origin-left after:scale-x-0 hover:after:scale-x-100 after:transition-transform after:duration-300" %>
              <%= link_to 'About', profile_about_path(@user.username), class: "relative focus:text-brand-400 hover:text-brand-400 transition-all duration-300 after:content-[''] after:absolute after:left-0 after:bottom-0 after:w-full after:h-[2px] after:bg-brand-400 after:origin-left after:scale-x-0 hover:after:scale-x-100 after:transition-transform after:duration-300" %>
              <% if @user == current_user %>
                <%= link_to 'Edit profile', profile_about_path(@user.username), class: "relative focus:text-brand-400 hover:text-brand-400 transition-all duration-300 after:content-[''] after:absolute after:left-0 after:bottom-0 after:w-full after:h-[2px] after:bg-brand-400 after:origin-left after:scale-x-0 hover:after:scale-x-100 after:transition-transform after:duration-300" %>
              <% end %>
            </div>
            <span class="inline-block w-full bg-brand-200 h-0.5 rounded-md"></span>
            <%= yield%>
          </div>
          <div class="col-span-12 md:col-span-4 p-4">
          <!-- DIVISON-->
          <div>
            <%= image_tag 'avatar.svg', size: "120x120" %>
            <h3 class="font-bold text-brand-400 mt-2"><%= @user.username%></h3>
            <span class="text-brand-300 ">37K followers</span>
            <p class="mt-3 text-brand-300">Whatever your goals for 2025, science shows being happier will make you dramatically more likely to reach them. Being depressed, quite obviously, won’t. </p>
            <div class="flex gap-2 mt-4">
              <div class="bg-brand-200 p-1 rounded-full flex items-center justify-center shadow-md">
                <%= link_to "https://www.google.com", { target: "_blank" } do %>
                  <%= image_tag 'website.svg', size: "24x24" %>
                <% end %>
              </div>
              <div class="bg-brand-200 p-1 rounded-full flex items-center justify-center shadow-md">
                <%= link_to "https://www.google.com", { target: "_blank" } do %>
                  <%= image_tag 'Linkedin.svg', size: "24x24" %>
                <% end %>
              </div>
              <div class="bg-brand-200 p-1 rounded-full flex items-center justify-center shadow-md">
                <%= link_to "https://www.google.com", { target: "_blank" } do %>
                  <%= image_tag 'x.svg', size: "24x24" %>
                <% end %>
              </div>
              <div class="bg-brand-200 p-1 rounded-full flex items-center justify-center shadow-md">
                <%= link_to "https://www.google.com", { target: "_blank" } do %>
                  <%= image_tag 'facebook.svg', size: "24x24" %>
                <% end %>
              </div>
            </div>
          </div>
          <!--DIVISON -->
          </div>
        </div>
      </div>
    </main>
    <%= render partial: "shared/footer"%>
  </body>
</html>
